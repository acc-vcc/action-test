name: Build (Windows)

on:
  push:
    branches: [feature/app5]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Qt + MinGW をインストール
      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '5.12.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_mingw73'

      # Qt MinGW を PATH の先頭に強制
      - name: Force PATH to use Qt MinGW only 
        run: | 
          echo "PATH=${{ env.Qt5_Dir }}\bin;$env:PATH" >> $env:GITHUB_ENV

      # CMake configure
      - name: Configure (CMake)
        run: |
          cmake -G "MinGW Makefiles" -S app5 -B build

      # Build
      - name: Build
        run: |
          cmake --build build -- -j4

      # exe のみ artifact として保存
      - name: Upload artifact (Windows exe)
        uses: actions/upload-artifact@v4
        with:
          name: qt-thread-csv-sample-windows
          path: build/*.exe
